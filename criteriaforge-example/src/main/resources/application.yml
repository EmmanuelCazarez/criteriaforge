spring:
  main:
    banner-mode: "off"
  datasource:
    url: jdbc:h2:mem:criteriaforge;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: false

logging:
  level:
    root: WARN
